export interface TokenNormal{
	token : string;
	setToken(token : string);
	getToken() : string;
	rol : number;
	setRol(rol : number);
	getRol() : number;
	user : number;
	setUser(user : number);
	getUser() : number;
}
